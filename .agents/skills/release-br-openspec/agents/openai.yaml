interface:
  display_name: "Release BR-OpenSpec"
  short_description: "Audit, prepare, publish, and finalize releases"
  default_prompt: "Use $release-br-openspec to audit the current release state and take the next safe release step."
